#Maven Java SE examples

#Deliverables
1. simple-jar: Simplest case or a library jar.
2. simple-exec-jar : Creation of an Uber executable jar, use of shade plugin.
3. (TODO) simple-dist: 
    - Creates an executable jar
    - Copies necessary libraries in a lib dir.
    - Creates a zip file including the above and some scripts.
4. multi-module: Aggregation of lib and app jars, a combination of simple-jar with simple-exec-jar.
# Tools used
- junit
- log4j
